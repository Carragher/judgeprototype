<!DOCTYPE html>

<html>
<head>

  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">

  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">

  <!-- jQuery library -->
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.0/jquery.min.js"></script>

  <!-- Latest compiled JavaScript -->
  <script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
   <link href="/assets/style.css?body=1" rel="stylesheet" />

  <title>Judicatum</title>

</head>
<body>

<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header" style = "width: 100%">
      <a class="navbar-brand" href="#">
        <i style="color: #C36A2D " class="fa fa-code"></i><h4 style="display: inline; color: #C36A2D "> Judging System</h4>
      </a>
      <div class="header-buttons">
        <%if @current_user != nil %>
            <div class="col-md-2" style="padding-top: 5px"><h4> <%=@current_user.username.to_s%> </h4></div>

            <div class="col-md-2"><%= button_to :Home , :home, class: "btn btn-default navbar-btn btn-color" %></div>
            <div class="col-md-2"><%= button_to :Profile , :profile, class: "btn btn-default navbar-btn btn-color" %></div>
            <div class="col-md-2"><%= button_to "Solution upload" , :setting, class: "btn btn-default navbar-btn btn-color" %></div>
            <div class="col-md-2"><%= button_to :Logout , :logout, class: "btn btn-default navbar-btn btn-color" %></div>
            <%  if @current_user.role == "admin"  %>
                <div class="col-md-2"><%= button_to :Admin, :admin, class: "btn btn-default navbar-btn btn-color" %></div>
                <div class="col-md-2"><%= button_to :Judge, :judge, class: "btn btn-default navbar-btn btn-color" %></div>

            <% end %>
        <%else%>
            <div class="col-md-6"><%= button_to :Signup , :signup, class: "btn btn-default navbar-btn btn-color"%></div>
            <div class="col-md-6"><%= button_to :Login , :login, class: "btn btn-default navbar-btn btn-color"%></div>
        <%end%>
        </div>
      </div>
  </div>
</nav>

<% if flash[:color]== "valid" %>
    <div class="valid_notice">
      <p><%= flash[:notice]%></p>
    </div>
<% elsif flash[:color]== "invalid"%>
    <div class="invalid_notice">
      <p><%=flash[:notice]%></p>
    </div>
<%else%>
    <div class="notice">
      <p><%=flash[:notice]%></p>
    </div>
<%end%>
<%= yield %>

</body>
</html>